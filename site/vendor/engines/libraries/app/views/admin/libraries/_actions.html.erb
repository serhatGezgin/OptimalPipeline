<ul>
  <% if Admin::LibrariesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_libraries_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_library_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::LibrariesController.sortable? and Library.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Libraries"),
                 admin_libraries_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Libraries"),
                 admin_libraries_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>